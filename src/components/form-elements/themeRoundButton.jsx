import { Button } from '@material-ui/core'
import React from 'react'

const ThemeRoundButton = ({
    color

}) => {
    return (
        <Button color={color} variant="contained">

        </Button>
    )
}

export default ThemeRoundButton